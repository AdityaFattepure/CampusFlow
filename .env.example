# Prisma database URL (SQLite local file).
# The running app is 100% client-side (localStorage) and does NOT query this
# database — it is only referenced by the unused src/lib/db.ts scaffold.
# Safe to leave as-is for Vercel deployment; set a real DB URL only if you
# later wire up the backend.
DATABASE_URL="file:./db/custom.db"
